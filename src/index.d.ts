/// Index file is for for creating an INTERFACE
/// INTERFACE is a cool concept of typescript which provides type safety

interface Currency {
    name : string;
    value : number;
    flag : string;
    symbol : string;   
}